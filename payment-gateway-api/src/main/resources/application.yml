server:
  port: 8082

spring:
  application:
    name: payment-gateway-api

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

springdoc:
  swagger-ui:
    path: /swagger-ui.html
